# WEATHER MASTER - Complete Implementation Plan

## 🎯 Project Overview
A smart weather app that provides weather data plus intelligent suggestions for clothing, activities, and daily planning based on current and forecasted conditions.

**Tech Stack:**
- Frontend: Next.js 14 (TypeScript) → Vercel
- Backend: FastAPI (Python) → Render
- Weather Data: OpenWeatherMap API

---

## 📁 Project Structure

### Frontend (Next.js) - `weather-master-frontend/`
```
weather-master-frontend/
├── src/
│   ├── app/
│   │   ├── layout.tsx                 # Root layout with metadata
│   │   ├── page.tsx                   # Home page (main weather interface)
│   │   ├── globals.css                # Global styles + Tailwind
│   │   └── api/                       # API route handlers (if needed)
│   ├── components/
│   │   ├── WeatherCard.tsx            # Current weather display
│   │   ├── ForecastCard.tsx           # Forecast items
│   │   ├── ClothingSuggestions.tsx    # What to wear section
│   │   ├── ActivitySuggestions.tsx    # Activities section
│   │   ├── SearchBar.tsx              # Location search
│   │   └── LoadingSpinner.tsx         # Loading state
│   ├── services/
│   │   └── api.ts                     # API calls to FastAPI backend
│   ├── types/
│   │   └── weather.ts                 # TypeScript interfaces
│   └── utils/
│       ├── formatters.ts              # Date/temp formatting
│       └── constants.ts               # API URLs, constants
├── public/
│   └── icons/                         # Weather icons
├── .env.local                         # Environment variables
├── next.config.js
├── tailwind.config.js
├── tsconfig.json
└── package.json
```

### Backend (FastAPI) - `weather-master-backend/`
```
weather-master-backend/
├── app/
│   ├── main.py                        # FastAPI app initialization
│   ├── config.py                      # Settings & env variables
│   ├── models/
│   │   └── weather.py                 # Pydantic models
│   ├── services/
│   │   ├── weather_service.py         # Weather API integration
│   │   ├── suggestion_service.py      # AI suggestions logic
│   │   └── geocoding_service.py       # Location lookup
│   ├── routers/
│   │   └── weather.py                 # API endpoints
│   └── utils/
│       └── helpers.py                 # Utility functions
├── requirements.txt
├── .env                               # Environment variables
└── Dockerfile                         # For Render deployment
```

---

## 🗄️ Data Models

### Backend Pydantic Models (models/weather.py)

```python
from pydantic import BaseModel
from datetime import datetime
from typing import List

class LocationRequest(BaseModel):
    location: str  # city name or coordinates

class WeatherResponse(BaseModel):
    location: str
    current_temp: float
    feels_like: float
    condition: str  # sunny, rainy, cloudy, etc.
    humidity: int
    wind_speed: float
    uv_index: int
    timestamp: datetime

class ForecastItem(BaseModel):
    date: str
    temp_high: float
    temp_low: float
    condition: str
    precipitation_chance: int

class ClothingSuggestion(BaseModel):
    category: str  # outerwear, top, bottom, footwear, accessories
    items: List[str]
    reason: str

class ActivitySuggestion(BaseModel):
    activity: str
    description: str
    suitability_score: int  # 1-10

class CompleteSuggestions(BaseModel):
    weather: WeatherResponse
    forecast: List[ForecastItem]
    clothing: List[ClothingSuggestion]
    activities: List[ActivitySuggestion]
```

### Frontend TypeScript Interfaces (types/weather.ts)

```typescript
export interface LocationRequest {
  location: string;
}

export interface WeatherResponse {
  location: string;
  current_temp: number;
  feels_like: number;
  condition: string;
  humidity: number;
  wind_speed: number;
  uv_index: number;
  timestamp: string;
}

export interface ForecastItem {
  date: string;
  temp_high: number;
  temp_low: number;
  condition: string;
  precipitation_chance: number;
}

export interface ClothingSuggestion {
  category: string;
  items: string[];
  reason: string;
}

export interface ActivitySuggestion {
  activity: string;
  description: string;
  suitability_score: number;
}

export interface CompleteSuggestions {
  weather: WeatherResponse;
  forecast: ForecastItem[];
  clothing: ClothingSuggestion[];
  activities: ActivitySuggestion[];
}
```

---

## 🔌 API Endpoints

### FastAPI Backend Routes

#### 1. Health Check
```
GET /api/health
Response: { "status": "healthy" }
```

#### 2. Current Weather
```
GET /api/weather/current?location={city_name}
Query Params: 
  - location (string, required)
Response: WeatherResponse
```

#### 3. Weather Forecast
```
GET /api/weather/forecast?location={city_name}&days={5}
Query Params:
  - location (string, required)
  - days (int, optional, default=5)
Response: List[ForecastItem]
```

#### 4. Complete Suggestions (Main Endpoint)
```
POST /api/suggestions
Body: { "location": "New York" }
Response: CompleteSuggestions
```

---

## 🧠 Core Logic & Algorithms

### Clothing Suggestions Algorithm (services/suggestion_service.py)

**Input Parameters:**
- Temperature (°F)
- Precipitation (rain/snow)
- Wind speed (mph)
- UV index
- Humidity

**Decision Logic:**

```python
def get_clothing_suggestions(weather_data):
    temp = weather_data.current_temp
    condition = weather_data.condition
    wind = weather_data.wind_speed
    uv = weather_data.uv_index
    
    suggestions = []
    
    # OUTERWEAR
    if temp < 32:
        suggestions.append({
            "category": "Outerwear",
            "items": ["Heavy winter coat", "Insulated jacket"],
            "reason": "Freezing temperatures"
        })
    elif temp < 50:
        suggestions.append({
            "category": "Outerwear",
            "items": ["Light jacket", "Sweater"],
            "reason": "Cold weather"
        })
    elif temp < 65:
        suggestions.append({
            "category": "Outerwear",
            "items": ["Light cardigan", "Hoodie"],
            "reason": "Cool temperature"
        })
    
    # TOP
    if temp > 85:
        suggestions.append({
            "category": "Top",
            "items": ["Light t-shirt", "Tank top", "Breathable fabric"],
            "reason": "Hot weather"
        })
    elif temp > 70:
        suggestions.append({
            "category": "Top",
            "items": ["T-shirt", "Short sleeve"],
            "reason": "Warm weather"
        })
    elif temp > 50:
        suggestions.append({
            "category": "Top",
            "items": ["Long sleeve shirt", "Light sweater"],
            "reason": "Mild weather"
        })
    else:
        suggestions.append({
            "category": "Top",
            "items": ["Thermal shirt", "Thick sweater"],
            "reason": "Cold weather"
        })
    
    # BOTTOM
    if temp > 75:
        suggestions.append({
            "category": "Bottom",
            "items": ["Shorts", "Light pants"],
            "reason": "Hot weather"
        })
    else:
        suggestions.append({
            "category": "Bottom",
            "items": ["Jeans", "Long pants"],
            "reason": "Cool weather"
        })
    
    # FOOTWEAR
    if "rain" in condition.lower() or "snow" in condition.lower():
        suggestions.append({
            "category": "Footwear",
            "items": ["Waterproof boots", "Rain boots"],
            "reason": "Wet conditions"
        })
    elif temp < 40:
        suggestions.append({
            "category": "Footwear",
            "items": ["Winter boots", "Warm shoes"],
            "reason": "Cold weather"
        })
    elif temp > 80:
        suggestions.append({
            "category": "Footwear",
            "items": ["Sandals", "Light sneakers"],
            "reason": "Hot weather"
        })
    else:
        suggestions.append({
            "category": "Footwear",
            "items": ["Sneakers", "Casual shoes"],
            "reason": "Comfortable weather"
        })
    
    # ACCESSORIES
    accessories = []
    if "rain" in condition.lower():
        accessories.append("Umbrella")
    if uv > 6:
        accessories.extend(["Sunglasses", "Sunscreen", "Hat"])
    if wind > 15:
        accessories.append("Windbreaker")
    if temp < 40:
        accessories.extend(["Gloves", "Scarf", "Beanie"])
    
    if accessories:
        suggestions.append({
            "category": "Accessories",
            "items": accessories,
            "reason": "Weather protection"
        })
    
    return suggestions
```

### Activity Suggestions Algorithm (services/suggestion_service.py)

**Input Parameters:**
- Weather condition
- Temperature
- Precipitation
- Wind speed
- Time of day (optional)

**Decision Logic:**

```python
def get_activity_suggestions(weather_data):
    temp = weather_data.current_temp
    condition = weather_data.condition.lower()
    wind = weather_data.wind_speed
    
    activities = []
    
    # OUTDOOR ACTIVITIES
    if "clear" in condition or "sunny" in condition:
        if 60 <= temp <= 85:
            activities.append({
                "activity": "Outdoor Picnic",
                "description": "Perfect weather for dining outdoors",
                "suitability_score": 10
            })
            activities.append({
                "activity": "Hiking",
                "description": "Great conditions for a nature walk",
                "suitability_score": 9
            })
            activities.append({
                "activity": "Cycling",
                "description": "Ideal temperature for bike riding",
                "suitability_score": 9
            })
        if temp > 85:
            activities.append({
                "activity": "Swimming",
                "description": "Hot weather perfect for pool or beach",
                "suitability_score": 10
            })
            activities.append({
                "activity": "Ice Cream Run",
                "description": "Cool down with a frozen treat",
                "suitability_score": 8
            })
    
    # RAINY DAY ACTIVITIES
    if "rain" in condition:
        activities.append({
            "activity": "Visit a Museum",
            "description": "Stay dry while exploring art and culture",
            "suitability_score": 9
        })
        activities.append({
            "activity": "Movie Theater",
            "description": "Cozy indoor entertainment",
            "suitability_score": 8
        })
        activities.append({
            "activity": "Read a Book at Café",
            "description": "Perfect weather for a hot beverage and reading",
            "suitability_score": 9
        })
    
    # FITNESS ACTIVITIES
    if wind < 10 and not ("rain" in condition or "snow" in condition):
        if 50 <= temp <= 75:
            activities.append({
                "activity": "Outdoor Running",
                "description": "Comfortable temperature for cardio",
                "suitability_score": 9
            })
        else:
            activities.append({
                "activity": "Indoor Gym Workout",
                "description": "Better to exercise indoors",
                "suitability_score": 7
            })
    else:
        activities.append({
            "activity": "Yoga or Home Workout",
            "description": "Indoor fitness option",
            "suitability_score": 8
        })
    
    # SOCIAL ACTIVITIES
    if 65 <= temp <= 80 and "clear" in condition:
        activities.append({
            "activity": "Outdoor Café",
            "description": "Perfect for meeting friends outside",
            "suitability_score": 9
        })
    
    # COLD WEATHER ACTIVITIES
    if temp < 40:
        if "snow" in condition:
            activities.append({
                "activity": "Skiing or Snowboarding",
                "description": "Great conditions for winter sports",
                "suitability_score": 10
            })
        activities.append({
            "activity": "Cozy Movie Night",
            "description": "Bundle up indoors with hot chocolate",
            "suitability_score": 9
        })
    
    # Sort by suitability score
    activities.sort(key=lambda x: x["suitability_score"], reverse=True)
    
    return activities[:5]  # Return top 5
```

---

## 🌐 External APIs

### OpenWeatherMap API Integration (services/weather_service.py)

**API Key Required:** Sign up at https://openweathermap.org/api

**Endpoints to Use:**

1. **Current Weather**
```
GET https://api.openweathermap.org/data/2.5/weather
Parameters:
  - q: city name (e.g., "London", "New York")
  - appid: YOUR_API_KEY
  - units: imperial (for Fahrenheit) or metric (for Celsius)
```

2. **5-Day Forecast**
```
GET https://api.openweathermap.org/data/2.5/forecast
Parameters:
  - q: city name
  - appid: YOUR_API_KEY
  - units: imperial or metric
```

3. **Geocoding (Optional)**
```
GET http://api.openweathermap.org/geo/1.0/direct
Parameters:
  - q: city name
  - limit: 5
  - appid: YOUR_API_KEY
```

**Sample Implementation:**

```python
import httpx
from app.config import settings

async def get_current_weather(location: str):
    async with httpx.AsyncClient() as client:
        response = await client.get(
            f"{settings.WEATHER_API_BASE_URL}/weather",
            params={
                "q": location,
                "appid": settings.WEATHER_API_KEY,
                "units": "imperial"
            }
        )
        response.raise_for_status()
        return response.json()

async def get_forecast(location: str, days: int = 5):
    async with httpx.AsyncClient() as client:
        response = await client.get(
            f"{settings.WEATHER_API_BASE_URL}/forecast",
            params={
                "q": location,
                "appid": settings.WEATHER_API_KEY,
                "units": "imperial"
            }
        )
        response.raise_for_status()
        return response.json()
```

---

## 🔐 Environment Variables

### Frontend (.env.local)
```env
# Development
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production (update after deploying backend)
# NEXT_PUBLIC_API_URL=https://weather-master-api.onrender.com
```

### Backend (.env)
```env
# OpenWeatherMap API
WEATHER_API_KEY=your_openweathermap_api_key_here
WEATHER_API_BASE_URL=https://api.openweathermap.org/data/2.5

# CORS Settings
CORS_ORIGINS=http://localhost:3000,https://weather-master.vercel.app

# Optional: Server settings
HOST=0.0.0.0
PORT=8000
```

---

## 📦 Dependencies

### Frontend (package.json)
```json
{
  "name": "weather-master-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/react": "^18.2.0",
    "@types/node": "^20.10.0",
    "tailwindcss": "^3.4.0",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "eslint": "^8.55.0",
    "eslint-config-next": "^14.0.0"
  }
}
```

### Backend (requirements.txt)
```txt
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
httpx==0.25.0
python-dotenv==1.0.0
```

---

## 🎨 Frontend Implementation Details

### API Service Layer (services/api.ts)

```typescript
import axios from 'axios';
import { CompleteSuggestions, WeatherResponse, ForecastItem } from '@/types/weather';

const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000';

const api = axios.create({
  baseURL: API_URL,
  headers: {
    'Content-Type': 'application/json',
  },
});

export const weatherApi = {
  getCurrentWeather: async (location: string): Promise<WeatherResponse> => {
    const response = await api.get(`/api/weather/current`, {
      params: { location },
    });
    return response.data;
  },

  getForecast: async (location: string, days: number = 5): Promise<ForecastItem[]> => {
    const response = await api.get(`/api/weather/forecast`, {
      params: { location, days },
    });
    return response.data;
  },

  getSuggestions: async (location: string): Promise<CompleteSuggestions> => {
    const response = await api.post(`/api/suggestions`, { location });
    return response.data;
  },
};
```

### Main Page Structure (app/page.tsx)

```typescript
'use client';

import { useState } from 'react';
import SearchBar from '@/components/SearchBar';
import WeatherCard from '@/components/WeatherCard';
import ForecastCard from '@/components/ForecastCard';
import ClothingSuggestions from '@/components/ClothingSuggestions';
import ActivitySuggestions from '@/components/ActivitySuggestions';
import LoadingSpinner from '@/components/LoadingSpinner';
import { weatherApi } from '@/services/api';
import { CompleteSuggestions } from '@/types/weather';

export default function Home() {
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [data, setData] = useState<CompleteSuggestions | null>(null);

  const handleSearch = async (location: string) => {
    setLoading(true);
    setError(null);
    try {
      const suggestions = await weatherApi.getSuggestions(location);
      setData(suggestions);
    } catch (err) {
      setError('Failed to fetch weather data. Please try again.');
      console.error(err);
    } finally {
      setLoading(false);
    }
  };

  return (
    <main className="min-h-screen bg-gradient-to-br from-blue-400 to-blue-600 p-4">
      <div className="max-w-6xl mx-auto">
        <h1 className="text-4xl font-bold text-white text-center mb-8">
          Weather Master
        </h1>
        
        <SearchBar onSearch={handleSearch} />
        
        {loading && <LoadingSpinner />}
        
        {error && (
          <div className="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
            {error}
          </div>
        )}
        
        {data && !loading && (
          <div className="space-y-6">
            <WeatherCard weather={data.weather} />
            <div className="grid grid-cols-2 md:grid-cols-5 gap-4">
              {data.forecast.map((item, idx) => (
                <ForecastCard key={idx} forecast={item} />
              ))}
            </div>
            <ClothingSuggestions suggestions={data.clothing} />
            <ActivitySuggestions suggestions={data.activities} />
          </div>
        )}
      </div>
    </main>
  );
}
```

---

## 🚀 Implementation Order (Step-by-Step)

### **PHASE 1: Backend Foundation (Start Here)**

#### Step 1: Project Setup
```bash
mkdir weather-master-backend
cd weather-master-backend
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
pip install fastapi uvicorn pydantic httpx python-dotenv
pip freeze > requirements.txt
```

#### Step 2: Create Basic Structure
- Create `app/` directory
- Create `app/main.py` with basic FastAPI app
- Create `app/config.py` for settings
- Create `.env` file with API keys

#### Step 3: Implement Models
- Create `app/models/weather.py`
- Define all Pydantic models

#### Step 4: Weather Service
- Create `app/services/weather_service.py`
- Implement OpenWeatherMap API integration
- Test with sample city

#### Step 5: Basic Endpoints
- Create `app/routers/weather.py`
- Implement `/api/health` endpoint
- Implement `/api/weather/current` endpoint
- Test with Postman or curl

### **PHASE 2: Backend Logic**

#### Step 6: Suggestion Service
- Create `app/services/suggestion_service.py`
- Implement clothing suggestion algorithm
- Implement activity suggestion algorithm

#### Step 7: Complete Endpoint
- Implement `/api/suggestions` endpoint
- Combine weather + suggestions
- Test thoroughly

#### Step 8: Error Handling
- Add try-catch blocks
- Add input validation
- Add proper HTTP status codes

### **PHASE 3: Frontend Foundation**

#### Step 9: Next.js Setup
```bash
npx create-next-app@latest weather-master-frontend --typescript --tailwind --app
cd weather-master-frontend
npm install axios
```

#### Step 10: Project Structure
- Create `src/types/weather.ts`
- Create `src/services/api.ts`
- Create `.env.local`

#### Step 11: Layout & Styling
- Update `app/layout.tsx`
- Configure Tailwind in `globals.css`
- Set up responsive container

### **PHASE 4: Frontend Components**

#### Step 12: Search Component
- Create `components/SearchBar.tsx`
- Implement location input
- Add search button

#### Step 13: Weather Display
- Create `components/WeatherCard.tsx`
- Display current weather data
- Add weather icons

#### Step 14: Forecast Display
- Create `components/ForecastCard.tsx`
- Show 5-day forecast
- Format dates properly

#### Step 15: Suggestions Display
- Create `components/ClothingSuggestions.tsx`
- Create `components/ActivitySuggestions.tsx`
- Add icons/visuals

#### Step 16: Loading States
- Create `components/LoadingSpinner.tsx`
- Add loading indicators
- Add error messages

### **PHASE 5: Integration**

#### Step 17: Connect Frontend to Backend
- Update API service with correct URL
- Test all API calls
- Handle errors gracefully

#### Step 18: Polish UI
- Add animations
- Improve responsive design
- Add weather-themed colors

#### Step 19: Testing
- Test all features
- Test error cases
- Test on mobile devices

### **PHASE 6: Deployment**

#### Step 20: Backend Deployment (Render)
1. Create `Dockerfile` in backend
2. Push code to GitHub
3. Create new Web Service on Render
4. Connect GitHub repo
5. Add environment variables
6. Deploy

#### Step 21: Frontend Deployment (Vercel)
1. Push code to GitHub
2. Import project to Vercel
3. Add environment variables
4. Deploy

#### Step 22: Final Configuration
- Update CORS settings in backend
- Update API URL in frontend
- Test production deployment

---

## 🐳 Deployment Files

### Backend Dockerfile
```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### Backend main.py (Complete Example)
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.config import settings
from app.routers import weather

app = FastAPI(title="Weather Master API", version="1.0.0")

# CORS
app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.CORS_ORIGINS.split(","),
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# Include routers
app.include_router(weather.router, prefix="/api", tags=["weather"])

@app.get("/")
async def root():
    return {"message": "Weather Master API"}

@app.get("/api/health")
async def health_check():
    return {"status": "healthy"}
```

### Backend config.py
```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    WEATHER_API_KEY: str
    WEATHER_API_BASE_URL: str = "https://api.openweathermap.org/data/2.5"
    CORS_ORIGINS: str = "http://localhost:3000"
    
    class Config:
        env_file = ".env"

settings = Settings()
```

---

## ✅ Testing Checklist

### Backend Tests
- [ ] Health endpoint returns 200
- [ ] Weather API integration works
- [ ] Invalid location returns proper error
- [ ] Suggestions generate correctly
- [ ] CORS headers are present
- [ ] All endpoints documented

### Frontend Tests
- [ ] App loads without errors
- [ ] Search functionality works
- [ ] Weather data displays correctly
- [ ] Forecast shows 5 days
- [ ] Clothing suggestions render
- [ ] Activity suggestions render
- [ ] Loading spinner shows during fetch
- [ ] Error messages display properly
- [ ] Responsive on mobile (375px width)
- [ ] Responsive on tablet (768px width)
- [ ] Responsive on desktop (1024px+ width)

### Integration Tests
- [ ] Frontend connects to backend
- [ ] All API calls successful
- [ ] Error handling works
- [ ] Production URLs configured

### Deployment Tests
- [ ] Backend deployed to Render
- [ ] Frontend deployed to Vercel
- [ ] Environment variables set
- [ ] Production API calls work
- [ ] HTTPS enabled
- [ ] No CORS errors

---

## 🎯 MVP Features (Core Requirements)

### Must Have
1. ✅ Search by city name
2. ✅ Display current weather (temp, condition, humidity, wind)
3. ✅ Show 5-day forecast
4. ✅ Generate clothing suggestions (4-5 categories)
5. ✅ Generate activity suggestions (3-5 activities)
6. ✅ Responsive design (mobile + desktop)
7. ✅ Error handling (invalid city, API errors)

### Should Have
- Weather icons for different conditions
- Temperature unit toggle (°F/°C)
- Loading animations
- Weather-themed UI colors

### Could Have (Future Enhancements)
- User location detection (geolocation)
- Save favorite locations (local storage)
- Weather alerts/warnings
- Historical weather data
- Hourly forecast
- Weather radar map
- Social sharing
- Packing list generator for trips
- Weather-based outfit photos/gallery
- Push notifications for severe weather
- Multiple language support
- Dark/light mode

---

## 🛠️ Troubleshooting Guide

### Common Issues

**Issue: CORS Error**
- Solution: Check CORS_ORIGINS in backend .env
- Ensure frontend URL is included
- Verify CORS middleware is configured

**Issue: API Key Invalid**
- Solution: Verify OpenWeatherMap API key
- Check if key is active (takes a few hours after signup)
- Ensure .env file is loaded correctly

**Issue: Location Not Found**
- Solution: Implement better error handling
- Suggest similar city names
- Add geocoding for better location search

**Issue: Slow API Response**
- Solution: Implement caching
- Use Redis for frequently searched cities
- Add loading timeouts

**Issue: Build Fails on Vercel**
- Solution: Check TypeScript errors
- Verify all dependencies in package.json
- Check Next.js version compatibility

**Issue: Backend Won't Start on Render**
- Solution: Verify start command
- Check Python version
- Review build logs for errors

---

## 📚 Resources

### Documentation
- [Next.js Docs](https://nextjs.org/docs)
- [FastAPI Docs](https://fastapi.tiangolo.com/)
- [WeatherAPI API](https://www.weatherapi.com/)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Vercel Deployment](https://vercel.com/docs)
- [Render Deployment](https://render.com/docs)

### Design Resources
- Weather Icons: [Font Awesome Weather](https://fontawesome.com/search?q=weather&o=r)
- Color Palettes: [Coolors.co](https://coolors.co/)
- UI Inspiration: [Dribbble Weather Apps](https://dribbble.com/search/weather-app)

---

## 🎓 Learning Outcomes

By completing this project, you will learn:
- Full-stack development with modern frameworks
- RESTful API design and implementation
- External API integration
- TypeScript for type-safe development
- Responsive web design with Tailwind CSS
- Cloud deployment (Vercel + Render)
- Environment variable management
- Error handling and validation
- Algorithm design for recommendations
- Project structure and organization
- GSAP for animations

---

## 📝 Notes for AI IDE
backend must have a separate folder, likewise for the frontend
### Implementation Tips
1. Start with backend first to ensure API works
2. Test each endpoint thoroughly before moving forward
3. Use TypeScript strictly for better IDE support
4. Keep components small and focused
5. Implement error boundaries in React
6. Use environment variables for all secrets
7. Add proper loading states for better UX
8. Write clean, commented code
9. Follow consistent naming conventions
10. Test responsive design at each breakpoint

### Code Quality Standards
- Use meaningful variable names
- Add JSDoc comments for functions
- Follow REST API naming conventions
- Implement proper HTTP status codes
- Use async/await for all API calls
- Handle all edge cases
- Add PropTypes or TypeScript interfaces
- Keep functions under 50 lines when possible
- Use constants for magic numbers
- Implement proper error logging

---

## 🚀 Quick Start Commands